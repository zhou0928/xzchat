# 新功能说明

## 版本 2.5.0 新增功能

本文档介绍 xzChat 2.5.0 版本新增的功能。

---

## 1. 对话导出 (`/export`)

### 功能描述
将当前对话历史导出为文件，支持 Markdown 和 JSON 两种格式。

### 用法

```bash
# 导出为 Markdown（默认）
/export

# 导出为 Markdown，指定文件名
/export md my-conversation.md

# 导出为 JSON
/export json history.json

# 查看帮助
/export help
```

### 特性

- ✅ 支持 Markdown 和 JSON 格式
- ✅ 自动生成文件名（带时间戳）
- ✅ 导出到 `~/Downloads/xzchat-exports/`
- ✅ 包含完整的元数据（会话名、时间、消息数）
- ✅ Markdown 格式带有角色标识和分隔线
- ✅ JSON 格式包含结构化数据

### 输出位置

```
~/Downloads/xzchat-exports/
├── xzchat-default-2026-01-30-09-30-00.md
└── xzchat-work-2026-01-30-09-31-00.json
```

---

## 2. 对话搜索 (`/find`)

### 功能描述
在历史对话中搜索特定内容，支持关键词、大小写和角色过滤。

### 用法

```bash
# 基本搜索
/find api

# 区分大小写
/find --case-sensitive XZChat

# 只搜索用户消息
/find --user 错误

# 只搜索助手回复
/find --assistant TypeScript
```

### 特性

- ✅ 关键词搜索
- ✅ 区分大小写选项
- ✅ 按角色过滤（用户/助手）
- ✅ 显示匹配次数
- ✅ 显示内容摘要（前 200 字符）
- ✅ 显示消息索引号

### 输出示例

```
✅ 找到 3 条匹配的消息:

1. 🤖 [#15] 在代码中使用 API 时，需要注意...
   匹配次数: 1

2. 👤 [#20] 请帮我处理 API 错误
   匹配次数: 2

3. 🤖 [#25] API Key 配置步骤如下...
   匹配次数: 1

💡 提示: 使用 /history 查看完整消息
```

---

## 3. 命令别名 (`/alias`)

### 功能描述
为常用命令创建简短的别名，提高使用效率。

### 用法

```bash
# 查看所有别名
/alias list

# 添加别名
/alias add cm /commit

# 删除别名
/alias remove cm

# 清空所有自定义别名
/alias clear

# 查看帮助
/alias help
```

### 内置别名

系统提供以下内置别名（无法删除）：

| 别名 | 对应命令 | 说明 |
|------|---------|------|
| `c` | `/clear` | 清空屏幕 |
| `h` | `/help` | 显示帮助 |
| `q` | `/quit` | 退出程序 |
| `e` | `/exit` | 退出程序 |
| `s` | `/scan` | 扫描项目 |
| `l` | `/load` | 加载文件 |
| `cm` | `/commit` | Git 提交 |
| `rv` | `/review` | 代码审查 |
| `hi` | `你好！我需要帮助` | 中文问候 |
| `bye` | `谢谢你的帮助` | 中文感谢 |
| `code` | `请帮我写一段代码：` | 代码提示 |
| `fix` | `请帮我修复以下错误：` | 错误修复 |
| `explain` | `请解释一下：` | 代码解释 |
| `summary` | `请总结一下：` | 内容总结 |

### 使用示例

```bash
# 输入别名会自动替换为完整命令
小周> cm           # 等价于输入 /commit
小周> s            # 等价于输入 /scan
小周> hi           # 等价于输入"你好！我需要帮助"
```

### 配置文件

自定义别名保存在：`~/.xzchat-aliases.json`

---

## 4. 预设提示词 (`/prompt`)

### 功能描述
管理预设提示词模板，快速插入常用的提示。

### 用法

```bash
# 列出所有预设
/prompt list

# 使用预设
/prompt use code-review

# 添加新预设
/prompt add my-prompt 这是我的自定义提示词

# 编辑预设
/prompt edit code-review

# 删除预设
/prompt remove my-prompt

# 导出所有预设
/prompt export

# 导入预设
/prompt import

# 查看帮助
/prompt help
```

### 内置预设

系统提供 7 个常用预设：

| 预设名 | 说明 |
|---------|------|
| `code-review` | 代码审查提示（检查 bug、性能、风格、安全） |
| `explain-code` | 代码解释提示（用简单易懂的方式解释） |
| `refactor` | 代码重构提示（提高可读性、优化性能） |
| `test` | 编写测试提示（正常、边界、异常情况） |
| `doc` | 编写文档提示（功能、参数、返回值、示例） |
| `translate` | 翻译提示（翻译成中文） |
| `debug` | 调试提示（分析原因并提供解决方案） |

### 使用示例

```bash
# 使用代码审查预设
小周> /prompt use code-review
✅ 已加载预设: code-review
📝 代码审查提示

请补充内容（或直接回车使用预设）:
> 请帮我检查这段代码...

# 或者输入预设名称直接使用
小周> code-review
（自动插入预设提示词）
```

### 配置文件

预设保存在：`~/.xzchat-presets.json`

---

## 使用场景示例

### 场景 1：代码审查工作流

```bash
# 1. 使用代码审查预设
/prompt use code-review

# 2. 添加代码内容
> 请帮我检查这段 React 组件...

# 3. AI 完成审查
🤖 发现以下问题：
1. 缺少错误处理...
2. Props 类型未定义...
```

### 场景 2：搜索历史对话

```bash
# 搜索特定的技术问题
/find TypeScript 类型定义

# 查看搜索结果
✅ 找到 2 条匹配的消息：

1. 🤖 [#10] TypeScript 接口定义应该放在 types/...
2. 👤 [#12] 请帮我修复 TypeScript 类型错误

# 查看完整消息
/history
```

### 场景 3：使用别名提高效率

```bash
# 设置常用命令别名
/alias add review /review
/alias add commit /commit

# 使用别名快速操作
小周> review     # 比 /review 更快
小周> commit     # 比 /commit 更快
```

### 场景 4：导出重要对话

```bash
# 完成重要讨论后导出
/export md important-discussion.md

# 查看导出结果
✅ 对话已导出到: /Users/username/Downloads/xzchat-exports/important-discussion.md
📊 导出 25 条消息
📁 格式: MD
```

---

## 技术细节

### 文件结构

```
bin/commands/
├── export.js        # 对话导出命令
├── find.js          # 对话搜索命令
├── alias.js         # 别名管理命令
└── prompt.js        # 预设提示词命令

lib/utils/
├── alias.js         # 别名管理工具类
└── presets.js       # 预设管理工具类
```

### 类设计

#### AliasManager

- `load()` - 加载别名配置
- `save()` - 保存别名配置
- `add(name, command)` - 添加别名
- `remove(name)` - 删除别名
- `get(name)` - 获取别名
- `list()` - 列出所有别名
- `has(name)` - 检查别名是否存在
- `clear()` - 清空所有别名

#### PresetManager

- `load()` - 加载预设
- `save()` - 保存预设
- `add(name, prompt, description)` - 添加预设
- `remove(name)` - 删除预设
- `get(name)` - 获取预设
- `list()` - 列出所有预设
- `clear()` - 清空所有预设
- `getDefaultPresets()` - 获取默认预设

---

## 未来计划

- [ ] 支持 `/history` 命令查看最近对话
- [ ] 增强 `/token` 命令显示实时统计
- [ ] 支持导出为 PDF 格式
- [ ] 支持别名参数传递
- [ ] 预设提示词分类管理

---

## 反馈

如果你有任何建议或遇到问题，请：

- 提交 Issue: https://github.com/zhou0928/xzchat/issues
- 讨论: https://github.com/zhou0928/xzchat/discussions

---

**Enjoy xzChat! 🚀**
